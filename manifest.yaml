name: dotnet-language-pack
healthEndpoints:
  liveness: /health/liveness
  readiness: /health/readiness
runtimes:
  - path: dotnet
    name: dotnet
    templates:
      - path: dotnet/http
        name: http
